{
  "name": "jihn-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "dev:all": "npm run dev:web & npm run dev:cli",
    "dev:cli": "npm run dev --workspace=apps/cli",
    "dev:web": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=packages/agent-core && npm run build --workspace=apps/cli && npm run build --workspace=apps/web",
    "test": "npm run test --workspace=packages/agent-core && npm run test --workspace=apps/cli",
    "lint": "npm run lint --workspace=packages/agent-core && npm run lint --workspace=apps/cli && npm run lint --workspace=apps/web"
  },
  "devDependencies": {
    "pnpm": "^10.9.0"
  }
}
