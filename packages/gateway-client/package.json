{
  "name": "@jihn/gateway-client",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "npm run build --workspace=@jihn/agent-core",
    "build": "tsc",
    "test": "npm run build && NODE_OPTIONS=--experimental-vm-modules jest --config jest.config.mjs",
    "lint": "eslint ."
  },
  "dependencies": {
    "@jihn/agent-core": "file:../agent-core",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "@types/ws": "^8.18.1",
    "jest": "^30.0.0",
    "typescript": "^5.9.3"
  }
}
