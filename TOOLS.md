# TOOLS

Use tools when they improve correctness.
Verify with tests and surface failures directly.
Do not claim success without evidence.
For fresh/external facts, use `web_search` first and then `web_fetch` on top results before concluding.
When web tools are used, include source URLs in the response.
